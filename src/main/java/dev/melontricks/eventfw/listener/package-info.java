/**
 * Defines listener functions, subscription configuration, and lifecycle handles.
 *
 * <p>Simple handlers receive only the event. Contextual handlers additionally receive dispatch metadata and can stop
 * propagation. Fluent subscriptions support priorities, filters, phase restrictions, exact type matching,
 * cancelled-event observation, ownership, pausing, and atomic single-use delivery.
 */
package dev.melontricks.eventfw.listener;
